package etcd

// Internal schema keys.
const (
	KeyCrypts      = "/crypts"
	KeyDHCP        = "/dhcp"
	KeyIPAM        = "/ipam"
	KeyLeaseUsages = "/lease-usages"
	KeyMachines    = "/machines"
	KeyNodeIndices = "/node-indices"
)
